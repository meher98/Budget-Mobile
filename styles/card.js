import { StyleSheet } from "react-native";
import { transparent_color } from "./vars.js";

export const cardStyles = StyleSheet.create({
  cardContainer: {
    width: "100%",
    padding: 10,
    backgroundColor: transparent_color,
    borderRadius: 10,
    flexGrow: 0,
  },
});
